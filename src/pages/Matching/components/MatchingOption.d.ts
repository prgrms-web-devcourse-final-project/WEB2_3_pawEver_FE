interface OptionProps {
    option: string;
    isSelected: boolean;
    onClick: () => void;
}
export default function MatchingOption({ option, isSelected, onClick, }: OptionProps): import("react/jsx-runtime").JSX.Element;
export {};
